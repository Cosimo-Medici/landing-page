# Ralph Progress Log
Started: 2026-03-20
---

## Codebase Patterns
- Site is a single HTML page (index.html) + separate CSS (css/styles.css) + separate JS (js/main.js)
- Light mode is DEFAULT (data-theme="light" on <html>). Dark mode is toggled via JS
- ALL colors use CSS custom properties defined in :root and [data-theme="light"] blocks in styles.css
- The chat demo frame must ALWAYS stay dark regardless of theme (same as the old terminal)
- Sections follow a consistent pattern: border-top: 1px solid var(--border), padding Xrem 2.5rem
- Labels are: IBM Plex Mono, ~0.6rem, letter-spacing 0.25em+, uppercase, accent-colored
- GSAP ScrollTriggers all use once: true and power3.out easing
- There is a word-by-word text reveal function (splitWordsAndAnimate) that targets specific heading selectors
- Mobile breakpoints: 1024px (tablet), 768px (large mobile), 640px (mobile), 380px (small mobile)
- The .term-dots and .term-dot CSS classes are SHARED between old terminal and new chat UI — do NOT remove them
- Lenis smooth scroll is integrated with GSAP via gsap.ticker — do NOT change the Lenis setup
- The light-mode CSS section starts around line 36 in styles.css and contains overrides for [data-theme="light"]
- The nav uses mix-blend-mode: difference (but this is turned off in light mode)
- GSAP hero animation is a timeline with delay 0.3, revealing status -> h-line 1 -> h-line 2 -> h-line 3 -> hero-bottom

---

## 2026-03-20 - US-001
- Rewrote hero section copy: headline → "The back office your fund deserves", status line → "Processing 50,000+ fund documents per month", h-sub → lists specific deliverables (LP reports, K-1 packages, rent roll extractions, due diligence memos), secondary CTA → "Watch it work ↓"
- Files changed: index.html (lines 29-43 only)
- **Learnings for future iterations:**
  - Hero structure is clean — only text content inside existing spans needs changing
  - No CSS or JS changes needed for text-only copy rewrites
  - The word "AI" was in the meta description too (not hero) — will need updating in US-012
---

## 2026-03-20 - US-002
- Added all chat demo CSS classes to css/styles.css: .chat-frame, .chat-chrome, .chat-tabs, .chat-tab, .chat-version, .chat-body, .chat-msg, .chat-avatar, .chat-bubble, .chat-context, .chat-citations, .chat-cite, .chat-artifact, .chat-thinking, .chat-input-bar, .chat-try-cta, @keyframes thinkPulse
- Light-mode overrides added to keep chat frame always dark (hardcoded dark colors, not using CSS vars)
- Mobile responsive rules at 640px: scrollable tabs, reduced padding, increased bubble max-width to 85%
- Files changed: css/styles.css only (CSS-only story, no HTML or JS changes)
- **Learnings for future iterations:**
  - Chat frame uses hardcoded dark colors (#141413, #1A1A18, #EDE8DF) instead of CSS custom properties since it must stay dark in both themes
  - Light-mode overrides for chat elements follow same pattern as old terminal overrides — force dark colors explicitly
  - The chat-try-cta hover uses #74418F directly since it's always on dark background
  - Mobile chat responsive rules mirror the existing term-tabs mobile pattern (overflow-x: auto, scrollbar-width: none)
---

## 2026-03-20 - US-003
- Replaced old terminal-frame HTML with new chat UI markup inside the terminal-stage wrapper
- New structure: chat-frame > chat-chrome (term-dots + chat-tabs + chat-version) + chat-body + chat-input-bar, plus chat-try-cta below
- Three tab buttons with data-demo attributes (no inline onclick handlers)
- Added data-lenis-prevent to chat-body for scroll isolation
- Files changed: index.html (lines 64-91)
- **Learnings for future iterations:**
  - The .terminal-stage wrapper was preserved — chat-frame lives inside it, so existing GSAP reveal for .terminal-stage still works
  - term-dots and term-dot classes are reused from old terminal chrome (shared CSS)
  - Tab interactivity deferred to US-004 (JS story)
  - chat-try-cta sits outside chat-frame but inside terminal-stage
---

## 2026-03-20 - US-004
- Replaced old terminal demo JS (demos array, demoRunning, runDemo, terminal ScrollTrigger) with new chat demo system
- New chatDemos array with 3 demos: Due Diligence, LP Reporting, Rent Roll — each with context string and 4 messages (2 user, 2 cosimo with citations and artifacts)
- New runChatDemo(index) function: clears chat body, shows context, plays messages sequentially (user = immediate fade-in, cosimo = thinking dots 1.2s then fade-in)
- Tab click handlers via querySelectorAll('.chat-tab')
- ScrollTrigger auto-starts demo 0 when .chat-frame enters viewport at 70%
- Uses requestAnimationFrame + .visible class for fade-in (vanilla, not GSAP)
- chatRunning flag prevents overlapping demos
- Files changed: js/main.js only
- **Learnings for future iterations:**
  - The .terminal-stage GSAP reveal (lines 100-104) still works for the new chat UI since .chat-frame lives inside .terminal-stage
  - Double requestAnimationFrame needed for CSS transition to trigger after DOM append
  - createChatMsg() helper builds the full message DOM including avatar, bubble, citations, and artifact card
---

## 2026-03-20 - US-005
- Removed old terminal CSS: .terminal-frame, .terminal-frame::before, .term-chrome, .term-tabs, .term-tab, .term-tab.active, .term-tab:hover, .term-version, .term-body, .term-body.active, .term-line, .term-line.visible, .t-cursor, .cursor-blink, @keyframes blink
- Removed terminal color utility classes: .p, .c, .d, .r, .g, .s, .gap
- Removed all [data-theme="light"] overrides for terminal classes
- Removed mobile responsive rules for .term-tabs, .term-tab, .term-body
- Removed .term-tab from utility cursor selector
- KEPT: .terminal-section, .terminal-intro, .terminal-stage, .term-dots, .term-dot (still used)
- Files changed: css/styles.css only
- **Learnings for future iterations:**
  - Always grep index.html before removing a CSS class to verify it's unused
  - The .term-dots/.term-dot classes are shared between old terminal and new chat chrome — important to preserve
---

## 2026-03-20 - US-006
- Replaced pain section stats grid with two-column before/after comparison layout
- Left column ("Today") has 6 dense steps describing manual LP reporting + summary "3 people. 3 weeks. Every quarter."
- Right column ("With Cosimo") has 3 clean steps + summary "1 person. 15 minutes. Same report."
- Purple divider between columns, accent-tinted left borders on "With Cosimo" steps
- Responsive: columns stack at 768px with horizontal divider, at 1024px divider hidden and 2-col grid
- Removed old CSS: .pain-grid, .pain-item, .pain-num, .pain-desc and their responsive rules
- GSAP animation updated: .pain-item → .pain-col with stagger 0.3
- Files changed: index.html, css/styles.css, js/main.js
- **Learnings for future iterations:**
  - The pain-comparison uses a 3-column grid (col / divider / col) — divider is hidden on mobile via display:none
  - .pain-step-summary uses border-top as separator, not border-left like regular steps
  - The "Today" column intentionally has more steps (6) than "With Cosimo" (3) for visual weight contrast
---

## 2026-03-20 - US-007
- Rewrote architecture section: header "Three things generic AI..." (was "ChatGPT"), card titles → "Your Data Stays Yours", "It Gets Smarter at Your Work", "The Work, Done"
- Card text rewritten for buyer trust: data segregation as architecture, compounding knowledge moat narrative, specific deliverables with audit-ready emphasis
- Files changed: index.html only (text content changes, no structure/CSS/JS changes)
- **Learnings for future iterations:**
  - Copy-only stories are the simplest — just text inside existing divs, no structure changes
  - Card numbers (001/002/003) and icons (◇/⟳/▣) are kept as-is per acceptance criteria
---

## 2026-03-20 - US-008
- Added .quotes-framing element above .quotes-grid with text "We don't name our clients. They prefer it that way."
- Rewrote all 3 quotes with operational specificity: K-1 prep (3 weeks → 2 days), associate retention (midnight → 7pm), implementation (3 months → 2 days)
- Detailed attributions added: role, fund size, team size, location
- CSS added: .quotes-framing (IBM Plex Mono, uppercase, accent-colored, centered, opacity 0.7)
- GSAP fade-in animation added for .quotes-framing triggered on .quotes-section enter
- Files changed: index.html, css/styles.css, js/main.js
- **Learnings for future iterations:**
  - Adding a new element inside an existing section is straightforward — just insert before the grid
  - The quotes-framing animation triggers on .quotes-section (the parent), while quote-card animation triggers on .quotes-grid — this creates a nice sequential reveal
---

## 2026-03-20 - US-009
- Added new founder's note section between quotes and use cases sections
- Section contains: .founder-label ("From the founders"), .founder-text (personal paragraph about building Medici from finance experience), .founder-sign ("— Eliot & the Medici team")
- CSS added: .founder-section (padding 5rem 2.5rem, border-top), .founder-content (max-width 640px), .founder-label (IBM Plex Mono, accent, uppercase), .founder-text (Space Grotesk, 1rem, line-height 1.8), .founder-sign (IBM Plex Mono, dimmed)
- Mobile responsive: padding reduces at 640px, added to section padding-override list
- GSAP scroll reveal: .founder-text fades in on .founder-section trigger
- Files changed: index.html, css/styles.css, js/main.js
- **Learnings for future iterations:**
  - New sections follow the same border-top pattern as all other sections
  - Remember to add new sections to the 640px mobile padding override list
  - Simple text-only sections need minimal CSS — just the container, label, text, and sign styles
---

## 2026-03-20 - US-010
- Added .case-persona div as first child of each .case-card with role-based labels (Investment Partners, Fund CFOs, IR & Fund Accounting, Fund CFOs & Partners)
- Rewrote card titles: "Regulatory Reporting" → "K-1s & Regulatory Filing", "Investor Relations" → "LP Reporting"
- Rewrote descriptions with more specificity (1,200-document data room, 4pm LP question scenario, covenant compliance)
- Updated stats: 90% → "2 hrs" (vs. 2 weeks), 800+ → "30 sec", updated labels
- CSS added: .case-persona (IBM Plex Mono, 0.5rem, uppercase, accent-colored, opacity 0.6)
- Files changed: index.html, css/styles.css
- **Learnings for future iterations:**
  - Persona labels are minimal CSS — just font, size, color, spacing
  - No responsive changes needed for inline persona text within existing card layout
  - No JS changes needed — existing GSAP animations for .case-card still work
---

## 2026-03-20 - US-011
- Redesigned CTA section as 3-step pilot timeline (Discovery → Onboarding → Live Pilot)
- Inevitability section was already positioned correctly (after use cases, before CTA)
- Replaced .cta-sub and .cta-cred with .pilot-timeline (3-col bordered grid), .pilot-confidence paragraph
- CTA label → "Start a Pilot", headline → "Four weeks. Your data. You'll know."
- Buttons: "Schedule a Discovery Call" + mailto link
- CSS: .pilot-timeline, .pilot-step, .pilot-step-num, .pilot-step-duration, .pilot-step-title, .pilot-step-desc, .pilot-confidence
- Responsive: steps stack at 768px with border-bottom instead of border-right
- GSAP: removed .cta-sub animation, added staggered .pilot-step reveal
- Removed old .cta-sub and .cta-cred CSS rules
- Files changed: index.html, css/styles.css, js/main.js
- **Learnings for future iterations:**
  - Pilot timeline uses same visual language as .arch-grid (bordered grid, no gaps)
  - The inevitability section was already in the right position — no move needed
  - When removing CSS rules for deleted elements, also remove their GSAP animations
---

## 2026-03-20 - US-012
- Updated meta description to remove "AI" and focus on outcomes: "fund operations infrastructure for private markets. LP reports, K-1 packages, due diligence memos, and rent roll extractions delivered in minutes."
- Verified all 4 nav anchors (#product, #architecture, #cases, #contact) correctly target their sections
- Verified .chat-body has data-lenis-prevent attribute (added in US-003)
- Files changed: index.html (meta description only)
- **Learnings for future iterations:**
  - Quick verification stories — grep for IDs and attributes to confirm
  - Meta description was 147 chars, under the 160 char limit
---

## 2026-03-20 - US-013
- Audited all GSAP animations in main.js against index.html — every selector targets an existing element
- Confirmed no orphaned animations (.pain-item, .cta-sub already removed in prior stories)
- Confirmed all new section animations present: .quotes-framing, .founder-text, .pain-col, .pilot-step, .terminal-stage (covers chat-frame)
- Fixed missing `ease: 'power3.out'` on .pain-label and .cta-label scroll animations
- Verified all ScrollTriggers use `once: true` (parallax scrub triggers correctly exempt)
- Verified all 4 splitWordsAndAnimate selectors target existing elements
- No logic errors found — no potential console errors on full page scroll
- Files changed: js/main.js (2 ease properties added)
- **Learnings for future iterations:**
  - When adding scroll animations, always include ease property explicitly rather than relying on GSAP default
  - Hero timeline animations intentionally use power2.out (lighter feel for above-the-fold), all scroll-triggered reveals use power3.out
---
