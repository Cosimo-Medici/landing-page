{
  "name": "medici-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "serve": "npx serve dist -l ${PORT:-3000}",
    "start": "npm run build && npm run serve"
  }
}
